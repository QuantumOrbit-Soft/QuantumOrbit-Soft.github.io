services:
  quantumorbit-platform:
    container_name: quantumorbit-platform
    image: node:22
    volumes:
      - ../src:/src
    working_dir: /src
    ports:
      - "3000:3000"
    command: sh -c "corepack enable && pnpm install && pnpm dev --host 0.0.0.0"
